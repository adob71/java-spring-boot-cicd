openssl pkcs12 -export -in fullchain.pem -inkey privkey.pem -out tomcat.p12
keytool -list -alias 1 -keystore tomcat.p12

./mvnw spring-boot:run
./mvnw clean package
java -jar target/greeting-api-0.0.1-SNAPSHOT.jar
